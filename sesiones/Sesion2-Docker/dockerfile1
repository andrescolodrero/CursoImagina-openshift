FROM python:3.8-slim-buster
# FROM crea una capa basada en una imagen Base0

WORKDIR /app

COPY readme.txt readme.txt
# añade ficheros a la imagen
RUN pip3 install -r readme.txt
# Crea la aplicación


COPY . .

CMD [ "python3", "-m" , "flask", "run", "--host=0.0.0.0"]
# COmando a ejecutar